#include <stdio.h>

intmain(){

int num1=0;
printf("iserisci il valore di num1");
scanf("%d", &num1);

if(num1%3==00){
    if(num1%5)
    ("num1 è divisibile sia per 3 che per 5");
}
    else{
        printf("num1 è divisibile per 3 ma non per 5");
    }
        {
            printf("num1 non è divisibile per 3");
            return 0;

    }

}
  
 
